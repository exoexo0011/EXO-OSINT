# Base URL of the EXO-OSINT FastAPI backend.
# Copy this file to `.env` (or `.env.local`) and adjust as needed.
# When unset, the app falls back to http://127.0.0.1:8000
VITE_API_URL=http://127.0.0.1:8000
